Der var senest kontakt til "{{ gateway.name }}" for {{ ago }} sekunder siden.

Seneste kontakt var {{ gateway.lastSeenAt|date("d-m-Y H:i:s") }}.

Årsagerne kan være strømsvigt, manglende netværksdækning eller at udstyret skal genstartes.

--------------------------------------------------------------------------------
Gateway information
--------------------------------------------------------------------------------
Navn: {{ gateway.name }}
ID: {{ gateway.gatewayId }}
Beskrivelse: {{ gateway.description }}
Placering: {% include 'mails/map_link.txt.twig' with { 'latitude': gateway.location.latitude, 'longitude': gateway.location.longitude, 'content': gateway.location.latitude ~ "," ~ gateway.location.longitude } %}

Du kan se opsætningen af gatewayen på: {{ url }}
